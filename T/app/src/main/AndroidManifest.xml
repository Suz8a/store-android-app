<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.t">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo_negro"
        android:label="Troquel"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name=".ui.login.Tracker"></activity>
        <activity android:name=".ui.login.Tracker_taller"></activity>
        <activity android:name=".ui.login.Tracker_tienda"></activity>
        <activity android:name=".ui.login.Reporte"></activity>
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="Troquel">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".trackerActivity" />
    </application>

</manifest>